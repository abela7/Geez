        <!-- Item -->
        <div class="bg-card border border-main rounded-lg p-4 flex items-center justify-between">
            <div class="flex items-center gap-md min-w-0">
                <div class="w-10 h-10 rounded-full bg-card border border-main flex items-center justify-center">
                    <i class="fas fa-user text-muted"></i>
                </div>
                <div class="min-w-0">
                    <div class="font-medium text-primary truncate">Sarah Johnson</div>
                    <div class="text-sm text-secondary truncate">{{ __('staff.waiter') }} · {{ __('staff.service') }}</div>
                </div>
            </div>
            <div class="hidden md:flex items-center gap-lg text-sm text-secondary">
                <div class="truncate">sarah.johnson@example.com</div>
                <div>+1 202 555 0198</div>
                <div class="employee-status active"><i class="fas fa-circle text-[10px]"></i> {{ __('common.active') }}</div>
            </div>
        </div>

        <div class="bg-card border border-main rounded-lg p-4 flex items-center justify-between">
            <div class="flex items-center gap-md min-w-0">
                <div class="w-10 h-10 rounded-full bg-card border border-main flex items-center justify-center">
                    <i class="fas fa-user text-muted"></i>
                </div>
                <div class="min-w-0">
                    <div class="font-medium text-primary truncate">Michael Chen</div>
                    <div class="text-sm text-secondary truncate">{{ __('staff.chef') }} · {{ __('staff.kitchen') }}</div>
                </div>
            </div>
            <div class="hidden md:flex items-center gap-lg text-sm text-secondary">
                <div class="truncate">michael.chen@example.com</div>
                <div>+1 202 555 0142</div>
                <div class="employee-status active"><i class="fas fa-circle text-[10px]"></i> {{ __('common.active') }}</div>
            </div>
        </div>

        <div class="bg-card border border-main rounded-lg p-4 flex items-center justify-between">
            <div class="flex items-center gap-md min-w-0">
                <div class="w-10 h-10 rounded-full bg-card border border-main flex items-center justify-center">
                    <i class="fas fa-user text-muted"></i>
                </div>
                <div class="min-w-0">
                    <div class="font-medium text-primary truncate">Amina Yusuf</div>
                    <div class="text-sm text-secondary truncate">{{ __('staff.cashier') }} · {{ __('staff.administration') }}</div>
                </div>
            </div>
            <div class="hidden md:flex items-center gap-lg text-sm text-secondary">
                <div class="truncate">amina.yusuf@example.com</div>
                <div>+251 91 234 5678</div>
                <div class="employee-status active"><i class="fas fa-circle text-[10px]"></i> {{ __('common.active') }}</div>
            </div>
        </div>

        <div class="bg-card border border-main rounded-lg p-4 flex items-center justify-between">
            <div class="flex items-center gap-md min-w-0">
                <div class="w-10 h-10 rounded-full bg-card border border-main flex items-center justify-center">
                    <i class="fas fa-user text-muted"></i>
                </div>
                <div class="min-w-0">
                    <div class="font-medium text-primary truncate">Daniel Tesfaye</div>
                    <div class="text-sm text-secondary truncate">{{ __('staff.supervisor') }} · {{ __('staff.service') }}</div>
                </div>
            </div>
            <div class="hidden md:flex items-center gap-lg text-sm text-secondary">
                <div class="truncate">daniel.t@example.com</div>
                <div>+251 91 111 2222</div>
                <div class="employee-status inactive"><i class="fas fa-circle text-[10px]"></i> {{ __('common.inactive') }}</div>
            </div>
        </div>
